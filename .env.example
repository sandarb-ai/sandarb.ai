# Sandarb Environment Configuration

# PostgreSQL (required). Run: npm run db:init-pg (or ensure sandarb-dev DB and tables exist).
# Then load sample data: npm run db:seed-pg (requires DATABASE_URL).
# POSTGRES_PASSWORD=sandarb
# DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@localhost:5432/sandarb-dev

# Optional: API key for protecting endpoints
# API_KEY=your-secret-api-key

# Optional: Enable debug mode
# DEBUG=true

# When running UI on port 4000 and API on port 4001 (npm run dev), the UI uses this to call the API.
# Leave unset when running a single server (e.g. npm run dev:ui only).
NEXT_PUBLIC_API_URL=http://localhost:4001

# Server configuration (used when running a single process)
# PORT=4000
# HOST=0.0.0.0

# OpenTelemetry (optional). When set, traces and logs are exported to an OTLP endpoint.
# OTEL_ENABLED=true
# OTEL_SERVICE_NAME=sandarb
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_TRACES_EXPORTER=otlp
# OTEL_LOGS_EXPORTER=otlp
